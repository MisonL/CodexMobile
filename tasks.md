# CodexMobile Tasks

| ID | 标题 | 内容 | 验收标准 | 审查要求 | 状态 | 标签 |
| --- | --- | --- | --- | --- | --- | --- |
| CM-CLI-001 | CLI dry-run 安装骨架 | 新增 npm bin 入口、CLI 用户目录解析、macOS LaunchAgent plist dry-run 生成、`doctor`、`status`、`install --dry-run` 基础命令；不写入系统自启配置，不改变现有 `npm start` 行为。 | `npm run test:cli` 通过；`node bin/codexmobile.mjs install --dry-run --json` 输出计划且不创建 LaunchAgent；`doctor --json` 和 `status --json` 可在无外部 API key、无真机场景运行；`npm start` 入口保持为 `node server/index.js`。 | 检查无 secret/token 输出；检查 dry-run 无系统副作用；检查 package bin 与发布文件边界；检查路径解析覆盖 macOS、Windows、Linux。 | 已完成 | 高优先级 |
| CM-CLI-002 | CLI 服务启停与日志管理 | 将本地 Node 服务抽象为 CLI 可管理的 `serve`、`start`、`stop`、`restart`、`logs`；日志写入用户级状态目录，同时保持源码开发模式可用。 | 相关单元测试通过；本机启动后 `status` 可识别 PID、端口和日志路径；停止命令只停止 CLI 管理的进程。 | 检查不会误杀非 CodexMobile 进程；检查 Windows PATH 去重逻辑保留；检查日志不包含密钥。 | 已完成 | 高优先级 |
| CM-CLI-003 | macOS LaunchAgent 安装与卸载 | 在显式 `install` 下写入用户级 LaunchAgent，支持启用、禁用、状态检查和卸载；默认仍需用户显式执行。 | dry-run 和真实写入路径测试通过；plist 内容可由 `plutil -lint` 校验；`uninstall` 默认保留用户数据。 | 检查不会静默写系统配置；检查删除用户数据必须二次确认；检查 LaunchAgent label 和路径稳定。 | 已完成 | 高优先级 |
| CM-CLI-004 | Relay connector 常驻配置 | CLI 支持保存、读取和脱敏展示 relay URL、relay secret、本地 URL，并可管理现有 Mac connector 启动参数。 | 配置读写测试通过；`status` 脱敏展示 relay 配置；现有 `npm run relay:mac` 行为不回归。 | 检查 secret 不进入源码、日志和 git；检查弱 secret 显式失败；检查现有 relay smoke 通过。 | 已完成 | 高优先级 |
| CM-CLI-005 | 文档、验证记录与跨平台边界 | 更新 README、docs/reviews 记录和后续 Windows/Linux 自启边界，明确可自动验证与需真机手测的范围。 | `npm run build`、`npm run smoke`、`npm run smoke:relay` 通过或记录真实跳过原因；文档包含 npm CLI 使用路径。 | 检查文档与 `tasks.md`、代码事实一致；检查没有夸大手机端、Tailscale、Space 或真实 Codex 子进程覆盖范围。 | 已完成 | 待审查 |
| CM-CLI-006 | [Code Review] CLI 阶段收口审计 | 审查 `CM-CLI-001` 到 `CM-CLI-005` 的 CLI 代码、测试、README、计划和审计记录，确认是否符合 `AGENTS.md`、任务验收和安全边界。 | 输出 `docs/reviews/CR-CM-CLI-006-2026-05-19.md`；运行可用验证命令；若发现严重问题，在 `tasks.md` 追加未开始修复任务。 | 对照 README、`docs/reviews/CR-CM-CLI-001-2026-05-19.md` 到 `CR-CM-CLI-005-2026-05-19.md`、代码复杂度、secret 脱敏、跨平台边界和 smoke 结果；审计默认不改业务代码。 | 已完成 | 待审查 |
| CM-CLI-007 | [Fix] Relay streaming 异常清理 | 修复 `server/relay-runtime-stream-response.js` 在 `http.response.chunk` 起始缺失或 sequence/bytes 校验失败时直接抛错但未 settle pending request 的风险，确保定时器、pending map 和 onChanged 状态被清理。 | 新增或更新 relay streaming 负向测试；异常 chunk 返回明确错误；pending request 不泄漏；`npm run smoke:relay` 通过。 | 检查不改变正常 `http.response.start/chunk/end` 顺序；检查错误路径可观测；检查不会静默吞掉协议错误。 | 已完成 | 高优先级 |
| CM-CLI-008 | [Fix] Review 反馈优化 | 修复 `codex/relay-phase1` 相对 `main` review 中确认的 stale PID 误杀风险和 Mac connector 无效 JSON 静默降级问题。 | stale PID 复用不会触发 kill；无效 JSON response 保留原始 text；新增相关单元测试；`npm run test:cli`、`npm run test:relay-mac`、`npm run smoke:relay` 通过。 | 检查 stop 仍能停止真实 CLI 管理进程；检查不会把无效 JSON 伪装成空对象成功；检查修复记录写入 `docs/reviews/`。 | 已完成 | 高优先级 |
| CM-CLI-009 | [Fix] Review 收尾优化 | 修复 `start` 对 stale PID 复用和 malformed state 的已运行误判，并让 macOS LaunchAgent `install` / `enable` 支持重复执行。 | 在支持命令行归属校验的平台上，PID 被其他进程复用或 state 结构不完整时 `start` 会重新启动；重复 install/enable 不因已加载服务失败；unexpected bootout failure 不被吞掉；新增相关单元测试；`npm run test:cli` 通过。 | 检查不会误杀非 CodexMobile 进程；检查 LaunchAgent 重载顺序稳定；检查 dry-run bootout 仅允许未加载或不存在类错误失败的说明；检查 README 和 review 记录同步。 | 已完成 | 高优先级 |
| CM-RELAY-010 | [Closeout] 全部剩余问题收口 | 处理当前确认的 PR/CI 呈现、Docker 运行态记录、超 300 行源码文件复杂度债，确保收尾状态可审计。 | 新增或更新收口计划与审计记录；所有源码文件不超过 300 行，若存在外部平台无法直接修复项需记录真实原因；`npm run build`、`npm run smoke`、`npm run smoke:relay`、`npm run test:space-verify`、`git diff --check` 通过。 | 检查不改变 relay 协议语义；检查 `.codexmobile/`、日志、截图不入 Git；检查 PR、CI、Docker、本地分支与远端分支状态记录真实。 | 已完成 | 高优先级 |
| CM-RELAY-011 | [Planning] Relay production readiness 计划与分支重命名 | 将当前后续目标从 `relay-phase1` 重新界定为生产化就绪，新增根目录计划并切换到更准确的分支名称；不改变业务代码。 | `relay-production-readiness.md` 存在且只包含仍需推进或外部验收的项；当前本地分支为 `codex/relay-production-readiness`；`git diff --check` 通过。 | 检查计划不把已完成 Phase 1、streaming、realtime tunnel 任务重复列为未完成；检查外部 provider、真机和产品决策门禁没有被标记为代码已完成。 | 已完成 | 高优先级 |
| CM-RELAY-012 | Realtime provider-ready 真实门禁 | 使用真实 realtime provider key 复验 `/ws/realtime` 从浏览器经 Space relay、Mac connector 到 provider 的 ready、断线和取消路径；无 key 时只记录外部阻塞。 | `npm run space:verify -- --url https://misonl-codexmobile-relay.hf.space --check-realtime --require-realtime-ready --require-mac --json` 通过；或新增 review 记录说明缺少 key 且 tunnel 语义已覆盖。 | 检查缺 key 时必须显式失败或跳过，不得伪装 `voice.realtime.ready`；检查日志不泄露 provider key、browser token 或 pairing code。 | 未开始 | 待外部凭证 |
| CM-RELAY-013 | iPhone PWA 真机回归 | 在真实 iPhone 上验证 PWA 安装、触控、键盘弹起、横竖屏、浅色/深色、Wi-Fi/蜂窝或 Tailscale/Space 网络切换和配对流程。 | 新增 `docs/reviews/CR-RELAY-IPHONE-PWA-<日期>.md`，记录设备、网络、Space URL、步骤、结果和失败项；关键路径全部通过或有明确缺陷任务。 | 检查不把桌面 viewport smoke 当作真机结论；检查截图、日志和 token 不进入 Git。 | 未开始 | 待真机 |
| CM-RELAY-014 | Explicit multi-Mac routing 契约设计 | 设计正式多 Mac 路由模型，明确 route id、浏览器选择 UI/API、token 与 route 绑定、连接抢占规则和歧义状态文案。 | 更新 relay 设计文档并新增 review 记录；包含兼容迁移路径，单 Mac 默认行为不回归；未选择 route 时错误码稳定。 | 检查不恢复静默替换 active Mac；检查不同 `connectorInstanceId` 并发连接不会随机转发；检查 API 契约可测试。 | 未开始 | 高优先级 |
| CM-RELAY-015 | Explicit multi-Mac routing 实现 | 基于 `CM-RELAY-014` 契约实现多 Mac connector 注册、route-bound token/request、浏览器选择入口和 smoke 覆盖。 | 两个 Mac connector fixture 同时在线时，浏览器可显式选择目标；请求按 route 转发；未选择或无权限 route 返回稳定错误；`npm run smoke:relay` 和相关测试通过。 | 检查单 Mac 路径兼容；检查 token 不跨 route 越权；检查断线、重连、迟到响应和 ambiguous route 可观测。 | 未开始 | 高优先级 |
| CM-RELAY-016 | 生产观测与长期审计 | 补齐长期公网使用需要的 metrics/export 或状态指标、结构化审计日志 schema、requestId 追踪、脱敏扫描和告警边界。 | 人为触发 Mac offline、rate limit、timeout、stream abort 后，指标和日志可复现；新增测试或脚本覆盖脱敏扫描；文档记录保留/删除策略。 | 检查 secret、browser token、pairing code、请求体和本地完整路径不进日志；检查 `/api/status` 仍只暴露安全字段。 | 未开始 | 高优先级 |
| CM-RELAY-017 | 公网多人安全加固 | 面向 hostile network 补齐 proxy trust、origin allowlist、CSRF/Origin 检查、token rotation/revocation 和 abuse response。 | 非可信 `x-forwarded-for` 不影响限流 key；跨 origin 请求被拒绝；token/secret rotation 演练可复现；相关安全测试通过。 | 检查不破坏 LAN/Tailscale/local HTTPS 兼容路径；检查失败必须显式返回稳定错误码；检查不新增静默回退。 | 未开始 | 高优先级 |
| CM-RELAY-018 | [Code Review] Relay production readiness 收口审计 | 审查 `CM-RELAY-012` 到 `CM-RELAY-017` 的实现、验证、文档和外部门禁状态，确认是否达到生产化就绪边界。 | 输出 `docs/reviews/CR-RELAY-PRODUCTION-READINESS-<日期>.md`；运行 `npm run build`、`npm run smoke`、`npm run smoke:relay`、`npm run test:space-verify`、`git diff --check`；严重问题追加修复任务。 | 对照 `AGENTS.md`、`relay-production-readiness.md`、relay 设计文档和 review 记录；审计默认不修改业务代码。 | 未开始 | 待审查 |
