# CR-CM-CLI-005-2026-05-19

## Scope

本审计记录 `CM-CLI-005` 的 CLI 文档、验证记录与跨平台边界收口。范围包括 README 中 npm CLI 使用路径、自动验证范围、用户级状态目录说明、Windows/Linux 自启边界，以及本轮主线验证结果。

本阶段不新增 CLI 功能，不做真实手机、Tailscale、Space authenticated connector 或真实 Codex 子进程完整对话验证。

## Verification

| Command | Exit code | Result |
| --- | --- | --- |
| `npm run test:cli` | 0 | 36 CLI tests passed. 覆盖 CLI 路由、用户级路径解析、LaunchAgent、进程启停保护、日志脱敏、relay config 读写与脱敏。 |
| `npm run test:space-verify` | 0 | 11 Space verifier tests passed. 这是 verifier 单元测试，不等同于真实 Space 部署验证。 |
| `npm run build` | 0 | Vite production build completed. |
| `npm run smoke` | 0 | Existing local service returned `Smoke ok: misondeiMac.local codex/gpt-5.5 synced=null`. |
| `npm run smoke:relay` | 0 | Relay smoke passed, including expected negative-path logs for offline Mac, invalid connector id, ambiguous Mac route, body limit, pending limit, and reconnect behavior. |
| `node bin/codexmobile.mjs doctor --json` | 0 | Node 24.14.0 passed; Codex config exists; HTTP 3321 was listening; HTTPS 3443 was available; Tailscale command returned `ENOENT` and was reported as skipped. |
| `node bin/codexmobile.mjs status --json` | 0 | Reported no CLI-managed process, no installed LaunchAgent, no saved relay config, and user-level runtime paths. |
| `node bin/codexmobile.mjs install --dry-run --json` | 0 | Returned macOS LaunchAgent dry-run plan without writing the real plist. |
| `node bin/codexmobile.mjs relay-config --json` | 0 | Reported no default user-level relay config at `/Users/mison/Library/Application Support/CodexMobile/relay.json`. |
| `git diff --check` | 0 | No whitespace errors. |
| README code fence count check | 0 | README has an even number of Markdown code fences. |

## Review Notes

- README now states the source checkout entrypoint (`node bin/codexmobile.mjs`) and npm package entrypoint (`npx codexmobile`).
- README explicitly distinguishes local automated verification from real phone, Tailscale, hosted Space, login restart, and real Codex subprocess validation.
- macOS LaunchAgent support is documented as implemented behind explicit `install`; Windows Task Scheduler and Linux user systemd are documented as not implemented in this branch.
- `CODEXMOBILE_HOME` documentation now matches the CLI user-level path behavior and points users to `status --json` for the resolved paths.
- No secret, token, `.env`, generated upload, or user-level runtime file was added to git.

## Skipped Boundaries

- iPhone PWA install and mobile Safari microphone permission.
- Phone-to-Mac Tailscale reachability.
- macOS logout/login or reboot self-start validation after real LaunchAgent install.
- Real authenticated HuggingFace Space connector using live browser token or pair code.
- Real Codex subprocess full conversation chain beyond the current `/api/status` smoke.
- Windows Task Scheduler install/uninstall.
- Linux user systemd install/uninstall.
