# CR-CM-RELAY-010-2026-05-20

## 范围

本记录覆盖 `CM-RELAY-010` 的最终收口：前端和服务端复杂度债、PR/CI 呈现、Docker relay 运行态、本地验证和外部门禁边界。

## 变更摘要

- `client/src/App.jsx` 拆到 300 行以内，保留为 UI 装配层。
- 新增 `client/src/hooks/`，拆出状态容器、relay rate-limit lock、项目和 session 操作、消息操作、turn polling、WebSocket runtime、语音对话控制器、录音和 realtime voice 处理。
- 保留既有 `server/`、`scripts/`、`cli/` 和 `client/src/styles/` 拆分结果，所有源码文件维持 300 行以内。
- 未改变 `/api/status`、`/api/chat/send`、`/ws`、`/ws/realtime`、pairing、rate limit、pending request、multi-Mac 拒绝和 relay streaming 错误语义。

## 复杂度状态

| Check | Result |
| --- | --- |
| `client/src/App.jsx` | 206 lines |
| `client/src/hooks/useProjectController.js` | 247 lines |
| `client/src/hooks/useAppWebSocket.js` | 238 lines |
| `client/src/hooks/useVoiceDialogController.js` | 217 lines |
| 全仓源码行数检查 | No `client/src`, `server`, `scripts`, or `cli` source file over 300 lines |

## 验证记录

| Command | Exit | Result |
| --- | ---: | --- |
| `npm run build` | 0 | Vite production build passed. |
| `npm run smoke` | 0 | Local service smoke passed. |
| `npm run smoke:relay` | 0 | Relay smoke passed, including offline, invalid connector, ambiguous route, body limit, pending limit, reconnect, streaming, and realtime tunnel paths. |
| `npm run test:space-verify` | 0 | 12/12 tests passed. |
| `find client/src/hooks server scripts cli ... node --check` | 0 | Changed hook files plus server, script, and CLI JavaScript syntax checks passed. |
| `git diff --check` | 0 | Passed. |
| Source line-count gate | 0 | No source file over 300 lines. |
| `docker build -t codexmobile-relay:20260520-closeout .` | 0 | Image rebuilt from the current workspace; Docker build ran `npm run build` successfully. |
| `npm run space:verify -- --url http://127.0.0.1:9790 --json` | 0 | Docker relay public verifier passed 4/4; authenticated checks skipped because no browser token or pair code was supplied. |

## Docker 运行态

| Item | Value |
| --- | --- |
| Image | `codexmobile-relay:20260520-closeout` |
| Container | `codexmobile-relay-docker-smoke` |
| URL | `http://127.0.0.1:9790` |
| `/api/status` | `mode=relay`, `relayState=pairing_required`, `macConnected=false` |
| Relay started at | `2026-05-20T04:16:11.821Z` |

Docker public verifier confirmed the rebuilt container serves the built PWA, safe relay status, explicit realtime HTTP upgrade error, and unauthenticated `pairing_required`.

## Browser Smoke

| Target | Viewport | Result |
| --- | --- | --- |
| `http://127.0.0.1:9790` | Chrome, mobile 390x844 | Pairing screen rendered; root mounted; input and connect button visible; horizontal overflow reported as `0`. |

## PR 与分支状态

| Item | Value |
| --- | --- |
| Branch | `codex/relay-phase1` tracking `fork/codex/relay-phase1` |
| Pre-closeout head | `18aea34 docs: record verifier hardening deployment` |
| PR | `https://github.com/RNG2018-mlxg/CodexMobile/pull/4` |
| PR state | `OPEN`, draft `false`, merge state `CLEAN` |
| Upstream status checks | Empty `statusCheckRollup`; fork/local verification remains the current evidence source. |

## 外部门禁

- Authenticated Docker or Space relay checks need a fresh browser token or pair code and a live Mac connector.
- Realtime provider-ready still requires a configured realtime voice API key. Current evidence covers tunnel semantics and explicit provider configuration failure, not provider-ready audio completion.
- iPhone PWA install, physical touch, keyboard, orientation, Wi-Fi/cellular switching, and long-running mobile usage remain manual device gates.
- Formal multi-Mac route selection UI/API remains out of scope; current behavior intentionally rejects ambiguous Mac connector replacement.

## 结论

`CM-RELAY-010` 的可本地完成项已收口：源码复杂度进入 300 行限制，核心 relay 协议边界未改变，本地构建、smoke、relay smoke、Space verifier tests、Docker public verifier 和浏览器渲染检查均通过。剩余项目依赖外部凭证、真实 provider 或真机条件，不能由本轮代码变更伪装为已覆盖。
